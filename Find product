#include <stdio.h>
int main()
{
    int n,i;
    unsigned long long answer=1;
    int a[1000];
    const unsigned int m = 1000000007;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d ",&a[i]);
        answer=(answer*a[i])%m;
    }
    printf("%lu",answer);
    return 0;
}
